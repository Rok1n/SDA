name: custom-windows-release

on:
  push:
    branches:
      - master
    paths:
      - "custom-release-trigger.txt"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: pnpm/action-setup@v4
        with:
          version: 9

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "8.0.x"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: packages/core

      - name: Install wasm-bindgen-cli
        shell: pwsh
        run: cargo install wasm-bindgen-cli --version 0.2.127 --locked

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build decoder core
        run: pnpm core:build

      - name: Build AirPods motion bridge
        shell: pwsh
        run: dotnet publish native/windows/SdaAirPodsBridge/SdaAirPodsBridge.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o native/windows/SdaAirPodsBridge/publish

      - name: Build original web UI
        run: pnpm --filter @sda/web build

      - name: Build System Audio web UI
        run: pnpm --filter @sda/web exec vite build --config vite.system.config.ts

      - name: Build original SDA player installer
        shell: pwsh
        run: pnpm --filter @sda/desktop exec electron-builder --win nsis --x64 --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: Build SDA System Audio installer
        shell: pwsh
        run: pnpm --filter @sda/desktop exec electron-builder --config electron-builder.system.yml --win nsis --x64 --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: "false"

      - name: List release artifacts
        shell: pwsh
        run: |
          Get-ChildItem apps/desktop/dist -File | Select-Object Name,Length
          Get-ChildItem native/windows/SdaAirPodsBridge/publish -File | Select-Object Name,Length

      - name: Upload Windows artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sda-custom-windows
          path: |
            apps/desktop/dist/*.exe
            native/windows/SdaAirPodsBridge/publish/SdaAirPodsBridge.exe
          if-no-files-found: error

      - name: Create rolling custom release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "custom-${{ github.run_number }}"
          $title = "SDA Custom Windows $tag"
          $files = Get-ChildItem apps/desktop/dist/*.exe | ForEach-Object { $_.FullName }
          gh release create $tag $files --target "${{ github.sha }}" --title $title --notes "Custom Windows build for Rok1n: original SDA player plus SDA System Audio monitor/control app. The System Audio installer includes the SDA AirPods AACP motion helper, reads real yaw/pitch from a compatible installed driver, and connects to the SDA GlobalAudio bridge when present." --prerelease
